import styled from 'styled-components'

export const Container = styled.div`
	.header {
		display: flex;
		flex-direction: row;
		justify-content: space-between;

		h4,
		span {
			font-size: 1.5em;
			font-weight: bold;
		}
	}
`
